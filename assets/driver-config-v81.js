// FORM 14.10.3 — recommendation evidence strength + loft/configuration + shaft starting fit
(function(){
'use strict';
const ENG=window.FORM_DRIVER_ENGINE_V80,E=window.FORM_DRIVER_EVIDENCE_V80;
if(!ENG||!E){console.error('FORM config requires Driver Engine/Evidence');return;}
const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
const r1=v=>Math.round(v*10)/10;
function metric(id){return state?.metrics?.[id]||{mode:'unknown',value:null};}
function quality(mode){return ({exact:1,range:.84,general:.62,unknown:0}[mode]||0);}
function answered(v){return v!==null&&v!==undefined&&v!==''&&v!=='unknown';}
function golferNow(){return typeof normalizedGolferV69==='function'?normalizedGolferV69():golfer();}
function profileStrength(){const required=[state?.start,state?.curve,state?.costly,state?.strike],base=required.filter(answered).length/required.length;if(state?.lm==='none'||!state?.lm)return clamp(.62+.25*base,0,1);const core=['speed','spin','aoa','launch'],mq=core.map(id=>{const m=metric(id);return m.mode==='unknown'?.18:quality(m.mode)*(answered(m.value)?1:.35)});return clamp(.58*base+.42*(mq.reduce((a,b)=>a+b,0)/mq.length),0,1);}
function recommendationEvidence(s){const golfer=profileStrength(),product=clamp(Number(s?.evidenceQuality)||0,0,1),productSupport=.45+.55*product,combined=clamp(.58*golfer+.42*productSupport,0,1);let label=combined>=.85?'Strong':combined>=.72?'Good':combined>=.60?'Developing':'Limited';if(productSupport<.75&&label==='Strong')label='Good';if(golfer<.70&&['Strong','Good'].includes(label))label='Developing';return {golfer:r1(golfer*100),product:r1(productSupport*100),combined:r1(combined*100),label};}
function numeric(id){const m=metric(id);return m.mode==='exact'&&answered(m.value)?Number(m.value):null;}
function representative(id){const m=metric(id);if(!m||!answered(m.value)||m.mode==='unknown')return null;if(m.mode==='exact')return Number(m.value);const maps={speed:{under75:72,'75-84':80,'85-89':87,'90-94':92,'95-99':97,'100-104':102,'105-109':107,'110-114':112,'115plus':118},ballSpeed:{under120:115,'120-129':125,'130-139':135,'140-149':145,'150-159':155,'160-169':165,'170plus':175},carry:{under180:170,'180-199':190,'200-219':210,'220-239':230,'240-259':250,'260-279':270,'280plus':290}};return maps[id]?.[m.value]||null;}
function classify(id){const m=metric(id);if(!m||m.mode==='unknown'||!answered(m.value))return null;if(id==='spin'){if(m.mode==='exact')return +m.value<2100?'low':+m.value>3000?'high':'mid';if(m.mode==='range')return ['under1500','1500-1749','1750-1999','2000-2249'].includes(m.value)?'low':['3000-3499','3500plus'].includes(m.value)?'high':'mid';return ['verylow','low'].includes(m.value)?'low':['high','veryhigh'].includes(m.value)?'high':m.value==='varies'?'varies':'mid';}if(id==='launch'){if(m.mode==='exact')return +m.value<11?'low':+m.value>17?'high':'mid';if(m.mode==='range')return ['under8','8-10','10-12'].includes(m.value)?'low':['16-18','18-20','20plus'].includes(m.value)?'high':'mid';return ['verylow','low'].includes(m.value)?'low':['high','veryhigh'].includes(m.value)?'high':m.value==='varies'?'varies':'mid';}return null;}
function loftFit(p){const launch=classify('launch'),spin=classify('spin'),aoa=numeric('aoa');let loft=10.5,reasons=[],conflict=false;if(launch==='low'){loft+=1;reasons.push('lower launch');}else if(launch==='high'){loft-=1;reasons.push('higher launch');}if(spin==='low'){loft+=.5;reasons.push('lower spin');}else if(spin==='high'){loft-=.5;reasons.push('higher spin');}conflict=(launch==='low'&&spin==='high')||(launch==='high'&&spin==='low');if(aoa!=null&&aoa<=-2){loft+=.5;reasons.push('downward attack angle');}else if(aoa!=null&&aoa>=4){loft-=.5;reasons.push('upward attack angle');}if(p?.player==='lowspin')loft+=.5;loft=Math.round(clamp(loft,8,12)*2)/2;const lo=clamp(loft-.5,8,12),hi=clamp(loft+.5,8,12);let reason=reasons.length?`Driven by ${reasons.slice(0,3).join(', ')}.`:'Neutral starting loft from the information provided.';if(conflict)reason+=' Launch and spin point in competing loft directions, so launch-monitor validation matters more than the nominal loft.';return {loft,range:`${lo.toFixed(1)}°–${hi.toFixed(1)}°`,reason,conflict};}
function shaftFit(){const m=metric('speed'),g=golferNow();let speed=numeric('speed'),source='your measured club speed',estimated=false;if(speed==null&&m.mode==='range'){speed=representative('speed');source='your reported club-speed range';}if(speed==null&&m.mode==='general'){speed=({'belowavg':82,'typical':92,'aboveavg':101,'fast':108,'veryfast':116})[m.value]||Number(g?.speed)||null;source='your reported speed profile';estimated=true;}const ball=representative('ballSpeed'),carry=representative('carry');if(speed==null&&ball){speed=ball/1.45;source=carry?'your reported ball speed and carry distance':'your reported ball speed';estimated=true;}if(speed==null&&carry){speed=carry/2.45;source='your reported carry distance';estimated=true;}if(speed==null&&Number(g?.speed)){speed=Number(g.speed);source='your golfer profile';estimated=true;}if(speed==null)return {flex:'Shaft fit needs more data',weight:'Test a broad range',note:'FORM has enough information to rank heads, but not enough speed information to narrow the shaft starting point responsibly.'};let flex='Regular',weight='55–65g';if(speed<80){flex='Senior / A';weight='45–55g';}else if(speed<92){flex='Regular';weight='50–60g';}else if(speed<105){flex='Stiff';weight='55–65g';}else{flex='X-Stiff';weight='60–70g';}let note=`Starting point informed by ${source}. Final flex, profile and weight should still be validated for transition, feel and delivery.`;if(estimated)note+=' FORM is using those inputs to estimate a practical club-speed window rather than treating them as a directly measured club-speed average.';return {flex,weight,note};}
function profileInsight(g){const launch=classify('launch'),spin=classify('spin'),strike=g?.strike,costly=g?.costly,bits=[];if(launch==='low'&&spin==='low')bits.push('Your delivery is a low-launch / low-spin profile, so FORM is prioritizing launch and spin preservation rather than chasing another low-spin head.');else if(launch==='high'&&spin==='high')bits.push('Your delivery is a high-launch / high-spin profile, so flight control and spin reduction carry more weight than raw forgiveness alone.');else if(launch==='low'&&spin==='high')bits.push('Your launch and spin are moving in opposite fitting directions, which makes configuration validation especially important.');else if(launch==='high'&&spin==='low')bits.push('Your high-launch / low-spin combination is unusual enough that FORM avoids forcing a simplistic loft-only correction.');if(strike==='toe')bits.push('Toe contact makes toe-side speed retention and stability more valuable than generic MOI claims.');else if(strike==='heel')bits.push('Heel contact makes heel-side retention and directional stability especially relevant.');else if(strike==='varied')bits.push('Across-face strike variability increases the value of stability and retention over a single center-strike speed number.');if(costly==='two_way')bits.push('A two-way miss reduces the value of strongly draw-biased heads because correcting one side can worsen the other.');return bits.slice(0,2);}
function componentMap(s){return Object.fromEntries((s?.components||[]).map(x=>[x.key,x]));}
function weightedDifferences(a,b){const A=componentMap(a),B=componentMap(b),keys=[...new Set([...Object.keys(A),...Object.keys(B)])];return keys.map(key=>{const x=A[key],y=B[key];if(!x||!y)return null;const delta=r1((x.impact||0)-(y.impact||0));return {key,label:x.label||y.label,delta,scoreDelta:r1((x.score||0)-(y.score||0)),weight:x.normalizedWeight||0};}).filter(Boolean).filter(x=>Math.abs(x.delta)>=.15).sort((x,y)=>Math.abs(y.delta)-Math.abs(x.delta));}
function separation(rows){if(!rows?.length)return {label:'No ranking',gap:null,text:'No eligible models were available.'};if(rows.length===1)return {label:'Single eligible fit',gap:null,text:'Only one eligible manufacturer result remains after the fitting constraints.'};const gap=r1(rows[0].s.overall-rows[1].s.overall),ev=recommendationEvidence(rows[0].s);if(gap<1.25)return {label:'Near-tie',gap,text:`Only ${gap.toFixed(1)} Fit points separate #1 and #2. Treat them as the same testing tier unless measured performance creates separation.`};if(gap<2.75)return {label:'Narrow lead',gap,text:`The leader is ${gap.toFixed(1)} Fit points ahead. FORM sees a preference, not enough separation to imply a decisive performance advantage.`};if(ev.combined<72)return {label:'Modeled lead',gap,text:`The leader is ${gap.toFixed(1)} Fit points ahead, but evidence is still developing. The ranking is useful for test order, not a guarantee.`};return {label:'Meaningful lead',gap,text:`The leader is ${gap.toFixed(1)} Fit points ahead with enough recommendation support to justify testing it first.`};}
function rewriteDistinctions(rows,cards){cards.forEach((card,i)=>{const box=card.querySelector('.result70Distinction');if(!box||!rows[i])return;const other=i===0?rows[1]:rows[i-1];if(!other)return;const lead=i===0?rows[i]:other,trail=i===0?other:rows[i],gap=r1(lead.s.overall-trail.s.overall),diff=weightedDifferences(lead.s,trail.s).filter(x=>x.delta>0).slice(0,3);if(gap<1.25){box.innerHTML=`<b>${i===0?'Why it is #1 — for now':'Why the club above is essentially tied'}</b><p>The gap is only ${gap.toFixed(1)} Fit points. ${diff.length?`The largest weighted separation is ${diff.map(x=>`${x.label} +${x.delta.toFixed(1)} impact`).join(' · ')}.`:'Current evidence does not support a meaningful category separation.'} Test both before treating the rank order as decisive.</p>`;return;}box.innerHTML=`<b>${i===0?'Why it leads':'Why the club above leads'}</b><p>${diff.length?diff.map(x=>`${x.label} +${x.delta.toFixed(1)} weighted impact`).join(' · '):`The ${gap.toFixed(1)}-point overall separation comes from several smaller weighted advantages rather than one dominant category.`}</p>`;});}
function currentReliability(g){const cur=ENG.currentScore(g),brand=g?.currentClub?.brand,model=g?.currentClub?.model||'',clean=model.replace(/\s*\(20\d{2}\)\s*/,'').trim(),exact=!!(brand&&model&&products.find(p=>p.brand===brand&&(p.model===clean||p.model===model))),year=E.yearFromLabel(model);if(cur.score==null)return {...cur,exact:false,year,label:'Insufficient benchmark',note:'FORM does not have enough product information to grade this current driver defensibly.'};if(exact)return {...cur,exact:true,year,label:'Direct product profile',note:'This current-driver benchmark uses the same FORM product profile used for current-generation recommendations.'};return {...cur,exact:false,year,label:'Limited modeled benchmark',note:`FORM inferred this ${year?year+' ':''}driver's design traits from the available model information. The score is useful for test order, not a purchase claim.`};}
function currentContext(g){const labels={great:'Very well',good:'Pretty well',mixed:'Mixed',poor:'Not well'},problemLabels={distance:'not enough distance',forgiveness:'forgiveness',dispersion:'dispersion',spin_high:'too much spin',spin_low:'too little spin',launch_high:'launch too high',launch_low:'launch too low',feel:'feel / sound',looks:'looks at address'},bits=[];if(labels[g?.current])bits.push(`On-course report: ${labels[g.current]}`);(g?.currentClub?.problems||[]).slice(0,3).forEach(x=>bits.push(problemLabels[x]||String(x).replace(/_/g,' ')));return bits;}
function rewriteCurrentBenchmark(g,rows){const box=document.querySelector('.current70');if(!box)return;box.classList.add('current81Separated');const panels=[...box.children],profile=currentReliability(g),first=panels[0],upgrade=panels[1];const firstLabel=first?.querySelector('span');if(firstLabel)firstLabel.textContent='Current-driver benchmark — separate from Fit Score';const firstMeta=first?.querySelector('em');if(firstMeta)firstMeta.textContent=profile.score==null?'Not enough product evidence':profile.exact?`${profile.score.toFixed(1)} / 100 · ${profile.label}`:`≈${Math.round(profile.score)} / 100 · ${profile.label}`;if(!upgrade)return;const best=rows?.[0];if(!best||profile.score==null){upgrade.querySelector('b').textContent='Test before replacing';upgrade.querySelector('em').textContent='FORM can rank new drivers, but it cannot make a defensible upgrade claim without a usable current-driver benchmark.';return;}if(profile.exact)return;const gap=r1(best.s.overall-profile.score),wins=profile.detail?ENG.compare(best.s,profile.detail).filter(x=>x.delta>=4):[];upgrade.querySelector('b').textContent=gap<2.5||!wins.length?'No clear modeled upgrade':'Worth a side-by-side test';upgrade.querySelector('em').textContent=gap<2.5||!wins.length?`The modeled gap is ${gap.toFixed(1)} Fit points, and the limited historical benchmark does not support a stronger equipment claim.`:`FORM models a ${gap.toFixed(1)}-point fit advantage, but the current-driver profile is inferred rather than directly evidenced. Use this to prioritize a test—not to justify a purchase without measured validation.`;}
function decorate(){const g=golferNow(),rows=ENG.winners(g),cards=[...document.querySelectorAll('#result80Grid .result70Card')];if(!cards.length)return;cards.forEach((card,i)=>{const row=rows[i];if(!row)return;const evidence=recommendationEvidence(row.s),loft=loftFit(row.p),shaft=shaftFit();card.querySelector('.fitConfig81')?.remove();const block=document.createElement('div');block.className='fitConfig81';block.innerHTML=`<div><span>Starting loft</span><b>${loft.loft.toFixed(1)}°</b><small>Test ${loft.range}. ${loft.reason}</small></div><div><span>Shaft starting point</span><b>${shaft.flex}${shaft.weight.startsWith('Test ')?'':` · ${shaft.weight}`}</b><small>${shaft.note}</small></div><div><span>Evidence strength</span><b>${evidence.label} · ${Math.round(evidence.combined)}%</b><small>Golfer profile ${Math.round(evidence.golfer)}% · Product evidence ${Math.round(evidence.product)}%. This is support for the recommendation, not the Fit Score.</small></div>`;card.querySelector('.result70Top')?.insertAdjacentElement('afterend',block);});rewriteDistinctions(rows,cards);}
window.FORM_DRIVER_CONFIG_V81={recommendationEvidence,loftFit,shaftFit,profileInsight,separation,currentReliability,currentContext,decorate};
})();