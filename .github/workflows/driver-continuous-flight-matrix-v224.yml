name: Driver Continuous Flight Matrix v224
on:
  push:
    branches: [main]
    paths:
      - 'tests/driver-continuous-flight-v224.js'
      - 'tests/driver-continuous-flight-matrix-v224.html'
      - 'tests/run-driver-continuous-flight-matrix-v224.mjs'
      - '.github/workflows/driver-continuous-flight-matrix-v224.yml'
  workflow_dispatch:
jobs:
  compare:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Playwright
        run: |
          npm init -y >/dev/null 2>&1
          npm install playwright@1.55.0
          npx playwright install --with-deps chromium
      - name: Start static server
        run: |
          python3 -m http.server 8080 >/tmp/form-http.log 2>&1 &
          sleep 2
      - name: Run corrected stable vs continuous matrix
        run: node tests/run-driver-continuous-flight-matrix-v224.mjs
      - name: Upload comparison
        uses: actions/upload-artifact@v4
        with:
          name: driver-continuous-v224-comparison
          path: tests/baselines/driver-continuous-v224-*.json
          if-no-files-found: error
